spring:
    application:
        name: fcat-center

server:
    port: 8961

eureka:
    client:
        registerWithEureka: true
        fetchRegistry: false
        serviceUrl:
          defaultZone: http://localhost:${server.port}/eureka/
